{
  "name": "acaris-docs-web",
  "version": "1.0.0",
  "private": true,
  "description": "Scalar-style API documentation website for Acaris.",
  "scripts": {
    "dev": "cd docs && bun run dev",
    "start": "cd docs && bun run start",
    "check": "cd docs && bun run check"
  }
}
